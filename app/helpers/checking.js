import { helper } from '@ember/component/helper';
import {inject as service } from '@ember/service';

export default helper(function checking(params/*, hash*/) {
  // @service
  // store=this.store;
  // console.log(params);
});
